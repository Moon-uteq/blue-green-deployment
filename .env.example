# Environment Configuration
PROJECT_NAME=blue-green-app
NODE_ENV=production

# React Environment Variables
REACT_APP_ENVIRONMENT=blue
REACT_APP_VERSION=1.0.0

# Docker Configuration
COMPOSE_PROJECT_NAME=blue-green-deployment

# DigitalOcean Server Configuration
DO_SERVER_IP=your-server-ip-here
DO_SSH_KEY_PATH=~/.ssh/id_rsa

# GitHub Actions (set these as repository secrets)
# DO_SSH_PRIVATE_KEY=your-ssh-private-key
# DO_SERVER_IP=your-server-ip